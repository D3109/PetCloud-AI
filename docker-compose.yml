services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: petcloud
      POSTGRES_PASSWORD: petcloud
      POSTGRES_DB: petcloud_ia
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
